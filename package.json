{
  "name": "claude-code-eclaw-channel",
  "version": "0.1.0",
  "description": "EClaw channel plugin for Claude Code",
  "type": "module",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}