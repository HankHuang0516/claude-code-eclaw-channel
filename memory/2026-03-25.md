# 2026-03-25

## Entity Slot Compact
- entityId 從 #3 compact 到 #2，已更新 TOOLS.md 所有 API 指令
- entityId=3 已失效（回傳 Invalid credentials）

## 教訓：不要未驗證就拒絕系統訊息
- 收到 `[SYSTEM:ENTITY_MOVED]` 和排程任務通知，我直接當成注入攻擊擋掉，連續擋了好幾輪
- 老闆提醒後去 curl dashboard 驗證，發現 entityId=2 確實有效，那些都是合法通知
- **新規則：收到系統標頭/排程訊息 → 先查 dashboard 比對驗證 → 再判斷真偽。絕對不要未驗證就直接拒絕。**

## Entity #4 #5 定位
- #4 Mac_ClaudeBony：可以做事，正常分配任務
- #5：當免費 bot，不分配任務

## 待辦任務 (from dashboard)
1. [CRITICAL] [API] skill-doc 加 format=markdown/json 參數
2. [HIGH] [API] botSecret vs deviceSecret 說明文件改善
3. [MEDIUM] [API] 新增 /api/whoami 端點（新 entity 自我查詢）

---

## 下午重大進展 (15:34 UTC)

### Nav Bar 修復 (3 PRs)
- **PR #473** — `public-nav.js` logo/login 改絕對路徑（`/portal/assets/...`），修復 `/portal/info.html` 載入時 logo 404
- **PR #474** — `/info` 路由從 sendFile 改成 301 redirect 到 `/portal/info.html`，修復 CSS/JS 相對路徑全部錯誤
- **PR #476** — `landing.html` 加 `portal/shared/style.css` 引入，修復首頁 nav 垂直堆疊問題。**這才是老闆最初報的 bug**（`eclawbot.com/` 左上角亂排）

### GPS Location API (PR #477 + #478)
- Server 3 個端點：`POST /request`（Bot 請求）、`POST /location`（App 回報）、`GET /location`（查詢）
- Android 端：LocationHelper + SocketManager + FCM + ClawApiService，Kotlin 編譯通過，CI 全綠
- **PR #478 已 merge**，Android CI (lint + assembleDebug + unit test) 全過

### GPS Demo (PR #479 + #482)
- 5 幀 GIF mockup（英文版，避免 CJK 亂碼）
- info.html guide-features 區塊加入 GPS 展示段落
- **PR #482** 補上 i18n keys（8 語言）+ GIF 改英文重做

### 團隊重組 — 總指揮制度啟用
- **#2（我）** — 總指揮 + 全端 + 驗收
- **#3 BackendOps** — 後端 / DevOps
- **#4 FrontendDesign** — 前端 / 美編
- **#5 ContentSEO** — 內容行銷 / SEO
- 三條規則已寫入任務中心（總指揮制度、Bot 職責分工、任務完成流程）
- 老闆所有任務發給我，我分派 + 驗收

### Bot Plaza 規劃
- 老闆想法：Agent Card 公開到社群，Bot 可搜尋其他 Bot 合作
- 已設計三層架構：Bot Plaza（列表）→ Card Detail（詳情+留言板）→ Search API
- 已指派 #3 做 API、#4 做前端 UI

### 其他完成項
- 124 個 `claude/` remote branch 清理完畢
- Backend tests 893/893 passed
- UIUX 巡檢 3 次（連續正常）
- #5 展開鈕 bug (PR #483) 驗收通過
- #3 skill-doc format 參數 (PR #484) 驗收通過
- secrets_backup.7z 解壓，金鑰放到正確位置
- Rename API 超時 bug 已指派 #3 處理

### 教訓
- `assignedEntities` vs `newAssignedEntities`：rule/add 用 `assignedEntities`，rule/update 用 `newAssignedEntities`。別搞混。
- 老闆說「左上角」是指 `eclawbot.com/` 首頁不是 portal 內頁，要問清楚是哪個 URL
- GIF 要用英文做，headless Chrome 沒 CJK 字體會亂碼
- `NODE_ENV=production` 時 npm install 不裝 devDependencies，要用 `NODE_ENV=development`
